This is a [Next.js](https://nextjs.org/) project bootstrapped with [`create-next-app`](https://github.com/vercel/next.js/tree/canary/packages/create-next-app).


## Getting Started

First, run the development server:

```bash
npm run dev
```

## Launch tests

```bash
npm run test
```

*Comments from Rémi*

Click on test suites.

Most tests are red, **don't panick!** It's normal, there have to be completed.
**Look into the console** to observe printed object, as well as validation failures!

Before commiting, also checking that `npm run dev` and `npm run build` run correctly.
